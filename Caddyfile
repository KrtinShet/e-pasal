# Baazarify Local Development Proxy
#
# Usage: caddy run --config Caddyfile
#
# Access:
#   - http://dashboard.localhost  → Merchant dashboard
#   - http://admin.localhost      → Platform admin
#   - http://api.localhost        → API server
#   - http://*.localhost          → Storefront (any other subdomain)
#
# Examples:
#   - http://mystore.localhost    → Storefront for "mystore"
#   - http://demo.localhost       → Storefront for "demo"

# Dashboard - Merchant portal
dashboard.localhost {
	tls internal
	reverse_proxy localhost:7102
}

# Admin - Platform administration
admin.localhost {
	tls internal
	reverse_proxy localhost:7101
}

# API - Backend services
api.localhost {
	tls internal
	reverse_proxy localhost:7100
}

# Storefront - Wildcard for all tenant stores
# Any subdomain not matching above routes here
*.localhost {
	tls internal
	reverse_proxy localhost:7103
}
